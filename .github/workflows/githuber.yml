name: GitHuber (Nightly)

on:
  schedule:
    - cron: '29 10 * * *'
  workflow_dispatch: 

jobs:
  execute-hunt:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Python 3.10 Engine
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: ⛽ Inject Fuel (Install Dependencies)
        run: pip install -r requirements.txt

      - name: 🚀 IGNITION! (Run Agent)
        env:
          GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
          # TARGET_TOPIC 已经被我们在 python 脚本里切断了，这里不用传了
          OBSIDIAN_PATH: "./"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python githuber.py  # <--- 这次千万别再写成 hunter.py 了！！！
      
      - name: 📦 Push to GitHub Repository
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "chore: auto-sync nightly intelligence reports" || echo "No new files to sync"
          git push